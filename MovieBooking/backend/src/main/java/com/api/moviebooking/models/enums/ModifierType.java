package com.api.moviebooking.models.enums;

public enum ModifierType {
    PERCENTAGE,     // Increase/decrease by percentage (e.g., 20% = 1.2)
    FIXED_AMOUNT          // Add/subtract fixed amount
}
